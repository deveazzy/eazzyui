# Direktori Dependensi (Dependencies Directories)
# Folder tempat paket-paket diinstal oleh manajer paket.
node_modules/
vendor/

# File Konfigurasi Lingkungan (Environment Configuration Files)
# File yang berisi variabel lingkungan atau kredensial sensitif.
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# File Log (Log Files)
# File-file log yang dihasilkan oleh aplikasi.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Direktori dan File Hasil Build/Kompilasi (Build/Compilation Output Directories and Files)
# File-file yang dihasilkan saat proses build atau kompilasi.
dist/
build/
out/
.next/ # Untuk proyek Next.js
public/build/ # Untuk output Webpack/Laravel Mix
*.css.map
*.js.map
*.min.css
*.min.js

# Cache dan Direktori Sementara (Cache and Temporary Directories)
# Folder untuk cache atau file sementara.
.cache/
tmp/
temp/
.pnpm-store/ # Jika menggunakan pnpm

# File Konfigurasi Editor/IDE (Editor/IDE Configuration Files)
# File-file yang dibuat oleh editor atau IDE Anda.
.idea/ # IntelliJ IDEA, WebStorm, PHPStorm
*.sublime-project
*.sublime-workspace
.DS_Store # macOS
Thumbs.db # Windows
ehthumbs.db # Windows
desktop.ini # Windows
*.swp # Vim swap files
*.swo # Vim swap files
*~ # Backup files from some editors

# File Testing (Testing Files)
# Direktori untuk laporan cakupan kode (code coverage).
coverage/

# File Upload Pengguna (User Uploads)
# Jika aplikasi Anda mengizinkan upload file, direktori ini biasanya tidak dilacak.
# Contoh: Jika Anda punya folder 'storage/app/public' untuk upload.
# storage/*.
# public/uploads/

# File Database (Database Files)
# File database lokal atau SQLite.
*.sqlite
*.sqlite3
/database/*.sqlite
/database/*.sqlite3

# File Lain-lain (Miscellaneous Files)
# File-file lain yang umumnya tidak perlu dilacak.
.project
.settings/
.classpath
.factorypath
.metadata/
etc/
public/assets/vendor/
.vscode/
src/